"""What the model sees, from the typed line up — the key of the project.

`syntax` owns the story's typed language and the one dispatch from a
stored turn to its wire text; `assembler` composes the turns and the
current scenes into the request; `cards` owns character cards as
context material — the archive, macro binding, the wire block (FILE
decoding lives with the other wild formats, in backend.formats).
"""
